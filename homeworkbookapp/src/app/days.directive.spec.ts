import { DaysDirective } from './days.directive';

describe('DaysDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysDirective();
    expect(directive).toBeTruthy();
  });
});
